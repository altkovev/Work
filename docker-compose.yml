version: '3'
services:
  n8n:
    image: n8n/n8n:latest
    ports:
      - "8080:8080"
    environment:
      - N8N_PORT=8080
      - NODE_ENV=production
    volumes:
      - ./n8n:/home/node/.n8n
    restart: always
